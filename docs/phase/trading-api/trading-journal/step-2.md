# Step 2: TradingJournal 도메인/애플리케이션 계층 구현 (TDD)
Assigned agent: fullstack-dev

## Working Directory
.worktrees/trading-api-trading-journal

## Files to Read
- CODEX.md
- docs/ADR.md
- docs/PRD.md
- docs/phase/trading-api/trading-journal/spec.md
- backend/trading-api/src/main/kotlin/com/papertrading/api/domain/model/TradingJournal.kt
- backend/trading-api/src/main/kotlin/com/papertrading/api/domain/event/ExecutionFilledEvent.kt
- backend/trading-api/src/main/kotlin/com/papertrading/api/application/order/ExecutionProcessor.kt
- backend/trading-api/src/main/kotlin/com/papertrading/api/infrastructure/persistence/TradingJournalRepository.kt

## Tasks
1. 테스트 먼저 작성:
   - `backend/trading-api/src/test/kotlin/com/papertrading/api/application/journal/TradingJournalAutoGenerationServiceTest.kt`
   - `backend/trading-api/src/test/kotlin/com/papertrading/api/domain/model/TradingJournalTest.kt`
2. 도메인 모델 정리:
   - `TradingJournal`에서 `account: Account` 직접 참조를 `accountId: Long`으로 전환.
   - `journalType`, `sentiment`를 enum/VO로 모델링.
   - `summaryJson`, `entrySourceExecutionId`, `lastAutoGeneratedAt`, `version` 필드 추가.
3. 애플리케이션 서비스 추가:
   - `TradingJournalAutoGenerationService` 클래스 생성.
   - 시그니처:
     - `fun autoGenerateFromExecution(executionId: Long, idempotencyKey: String): AutoJournalResult`
   - 규칙:
     - 동일 `orderId + journalType` 중복 시 upsert.
     - 금액 계산은 `BigDecimal`만 사용.
4. 저장소 계약 확장:
   - `TradingJournalRepository`에 아래 시그니처 추가.
     - `fun findByOrderIdAndJournalType(orderId: Long, journalType: JournalType): TradingJournal?`
     - `fun findAllByAccountIdAndCreatedAtBetweenOrderByCreatedAtDesc(accountId: Long, from: Instant, to: Instant, pageable: Pageable): Page<TradingJournal>`
5. `ExecutionProcessor` 연동 포인트를 준비:
   - 직접 구현은 step-3에서 하되, 이벤트/호출 훅이 들어갈 위치 주석 또는 TODO 태그로 명시.

## Acceptance Criteria
```bash
cd backend/trading-api
./gradlew test --tests "*TradingJournalAutoGenerationServiceTest"
./gradlew test --tests "*TradingJournalTest"
./gradlew compileKotlin
```

## Agent Return Protocol
---
## Completion Report
- Status: PASS | FAIL | BLOCKED
- Summary: <one or two sentences of what was done>
- Files modified: <list of paths relative to main repo root>
- Blockers: <none | description>
---
